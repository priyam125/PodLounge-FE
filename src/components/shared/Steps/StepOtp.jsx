import Button from "../Button"

const StepOtp = ({onNext}) => {
  return (
    <div>
        <div>StepOtp</div>
        <Button text="Next" onClick={onNext} />
    </div>
  )
}

export default StepOtp